# SenseVoice Vibe — Python 依赖（venv 内）
# 安装：pip install -r requirements.txt

# ASR 引擎
funasr==1.3.1
funasr-onnx==0.4.1

# 模型下载
modelscope>=1.34.0
huggingface_hub>=0.20

# 语音活动检测
webrtcvad-wheels==2.0.14

# 推理后端（CPU 默认；GPU 自行替换 --index-url 为 cu121）
torch==2.10.0+cpu
torchaudio==2.10.0+cpu

# 数值计算
numpy>=2.0
