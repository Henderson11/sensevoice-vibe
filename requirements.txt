# SenseVoice 语音输入系统 - Python 依赖（venv 内）
# 安装: pip install -r requirements.txt
#
# 注意：IBus engine (ibus-sensevoice/sensevoice_engine.py) 用 /usr/bin/python3，
# 不在 venv 内，需要系统 apt 装 python3-gi + gir1.2-ibus-1.0（不在此处）

# === ASR ===
funasr==1.3.1
funasr-onnx==0.4.1            # ASR ONNX INT8 后端（model_quant.onnx 用）

# === 模型下载 ===
modelscope>=1.34.0            # 主下载（国内快）
huggingface_hub>=0.20         # fallback 下载

# === VAD ===
webrtcvad-wheels==2.0.14

# === PyTorch（CPU；GPU 用户自行换 --index-url）===
torch==2.10.0+cpu
torchaudio==2.10.0+cpu

# === 音频处理 ===
soundfile==0.13.1
numpy>=2.0

# === LLM 后处理（OpenAI 兼容协议）===
# 不需要 openai SDK——sensevoice/llm/postprocessor.py 直接用 urllib.request 调 HTTP API
