# SenseVoice 语音输入系统 - Python 依赖
# 安装: pip install -r requirements.txt

# ASR 模型
funasr==1.3.1

# 声纹验证

# VAD
webrtcvad-wheels==2.0.14

# PyTorch (CPU 版本，如需 GPU 请自行安装对应版本)
torch==2.10.0+cpu
torchaudio==2.10.0+cpu

# 音频处理
soundfile==0.13.1
numpy>=2.0

# LLM 后处理 (OpenAI 兼容 API)
openai>=1.0
modelscope>=1.34.0  # 模型下载
